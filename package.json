{
  "scripts": {
    "server": "cd server && nodemon server",
    "redux": "cd redux && yarn start",
    "start": "concurrently --kill-others-on-fail \"yarn server\" \"yarn redux\""
  },
  "dependencies": {
    "concurrently": "^7.0.0",
    "express": "^4.17.2",
    "mongoose": "^6.2.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
